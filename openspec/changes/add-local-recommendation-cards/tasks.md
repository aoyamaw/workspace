## 1. 后端推荐 API 与数据库

- [x] 1.1 定义推荐 DTO，包含类别、排名、名称、介绍、图片 URL、图片替代文本、来源标题、来源 URL、推荐项 ID 和批次 ID。
- [x] 1.2 新增推荐数据表迁移，保存地区标识、地区展示名、类别、推荐内容、来源、图片、内容指纹、批次关系和创建/更新时间。
- [x] 1.3 新增本地推荐查询接口，接收当前搜索地区标识和展示名，并返回美食与游玩地点推荐批次。
- [x] 1.4 新增“换一批”接口，接收当前地区、当前批次或已展示推荐项 ID，并返回不重复的新批次。
- [x] 1.5 实现 AI 推荐服务，按结构化 JSON 契约为地区生成 5 个美食和 5 个游玩地点。
- [x] 1.6 校验并规范化 AI 结果，包括连续排名、HTTP(S) 来源 URL、介绍长度、重复内容指纹、缺失图片回退信息。
- [x] 1.7 实现数据库推荐池复用逻辑，多用户请求同一地区时优先复用已有有效推荐项。
- [x] 1.8 实现推荐池扩充逻辑，当“换一批”无法从数据库取到足够不重复内容时调用 AI 生成并持久化新内容。
- [x] 1.9 增加超时、错误、无更多推荐和不支持地区处理，确保推荐失败不会影响天气查询。

## 2. 前端数据流

- [x] 2.1 增加本地推荐响应和推荐卡片项的 TypeScript 类型。
- [x] 2.2 增加推荐 API 调用逻辑，在天气搜索解析出当前地区后加载推荐内容。
- [x] 2.3 增加“换一批”请求逻辑，只刷新推荐区域，不重新查询天气。
- [x] 2.4 独立维护推荐加载、刷新中、错误、空状态、无更多推荐和重试状态。
- [x] 2.5 防止地区切换或连续刷新时旧请求覆盖新地区或新批次内容。

## 3. 推荐卡片 UI

- [x] 3.1 制作推荐卡片样式预览，使用模拟数据展示美食推荐、游玩地点推荐和右上角白底“换一批”刷新按钮。
- [x] 3.2 通过 Chrome 辅助功能向用户展示样式预览，等待用户确认样式可接受。
- [x] 3.3 在用户确认样式后，在主仪表盘新增本地推荐区域，包含美食推荐和游玩地点推荐两个 section。
- [x] 3.4 按确认后的样式在推荐区域右上角新增白底“换一批”按钮，文字右侧使用刷新图标。
- [x] 3.5 按确认后的样式将每个推荐 section 渲染为 1-5 排名的有序列表，卡片包含图片、标题、简短介绍和来源超链接。
- [x] 3.6 增加图片加载失败回退，保持卡片尺寸稳定并保留可访问替代文本。
- [x] 3.7 实现桌面和移动端响应式样式，避免嵌套卡片、文字重叠和横向滚动。
- [x] 3.8 确保来源链接和“换一批”按钮有可访问名称、可见焦点状态和安全链接属性。

## 4. 测试与验证

- [x] 4.1 增加后端测试，覆盖推荐查询成功、数据库复用、换一批去重、AI 扩充推荐池、重复内容指纹过滤和来源 URL 校验。
- [x] 4.2 增加后端测试，覆盖 AI 服务失败、无更多推荐、不支持地区和推荐数据持久化失败时的受控响应。
- [x] 4.3 增加前端测试，mock 推荐接口并验证搜索地区后两个 5 项排名推荐 section 正常渲染。
- [x] 4.4 增加前端测试，覆盖“换一批”按钮展示、刷新中状态、新批次替换、失败保留当前批次和无更多推荐提示。
- [x] 4.5 更新 Playwright 布局覆盖，在桌面和移动端包含推荐卡片与“换一批”按钮，并断言无横向溢出。
- [x] 4.6 用户确认推荐卡片样式后，使用 Chrome 辅助功能展示正式推荐区域的标题结构、图片替代文本、链接名称、按钮名称、键盘焦点和响应式样式。
- [x] 4.7 运行 OpenSpec 状态或校验命令，确认变更文档和任务在实现前没有未解决缺口。
